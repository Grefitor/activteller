import { RippleAnimationDirective } from './ripple-animation.directive';

describe('RippleAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new RippleAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
